<?xml version="1.0" encoding="utf-8"?>
<!--
    Screen transitions. The release design keeps navigation calm: the outgoing
    screen fades while the incoming one settles in from the side, so pushing and
    popping screens reads as one continuous motion instead of a hard cut.
-->
<set xmlns:android="http://schemas.android.com/apk/res/android"
    android:shareInterpolator="false">
    <translate
        android:duration="300"
        android:fromXDelta="6%p"
        android:interpolator="@android:anim/decelerate_interpolator"
        android:toXDelta="0" />
    <alpha
        android:duration="300"
        android:fromAlpha="0.0"
        android:interpolator="@android:anim/decelerate_interpolator"
        android:toAlpha="1.0" />
</set>
